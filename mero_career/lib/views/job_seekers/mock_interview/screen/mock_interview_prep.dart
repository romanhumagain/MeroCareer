import 'package:flutter/material.dart';

class MockInterviewPrep extends StatelessWidget {
  const MockInterviewPrep({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Mock Interview Preparation Page"),
      ),
    );
  }
}
